import ComingSoon from '../components/ComingSoon.jsx';

// PRD screen 18 (Sales, Revenue & Profit Analytics) — no
// /admin/analytics/summary|revenue-by-month|top-agencies routes exist in
// this backend build yet.
export default function Analytics() {
  return (
    <ComingSoon
      title="Analytics"
      description="Sales, revenue, profit margin, and top-agency dashboards will live here."
    />
  );
}
