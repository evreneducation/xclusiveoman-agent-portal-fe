# Used only if NOT going through the Vite dev proxy (e.g. a separately hosted API).
# Leave unset in local dev — vite.config.js proxies /api and /socket.io to the backend.
VITE_API_PROXY_TARGET=http://localhost:4000
