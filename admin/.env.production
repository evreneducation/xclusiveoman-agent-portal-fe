# Loaded automatically by `vite build` (mode=production) only — local `npm run dev`
# keeps using `.env` / the Vite proxy. See src/api/client.js and src/lib/socket.js.
VITE_API_PROXY_TARGET=https://xclusiveoman-agent-portal-be.onrender.com
